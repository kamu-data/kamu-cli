version: 1
kind: DatasetSnapshot
content:
  name: testing.empty-deriv
  kind: derivative
  metadata:
    - kind: setTransform
      inputs:
        - name: testing.empty-root
      transform:
        kind: sql
        engine: flink
        query: |
          select * from `testing.empty-root`
